# PROVIDER REGISTER

## DIGITAL HUMAN PROVIDER
- **Provider:** Missing (TBD pending credential activation).
- **Adapter/Service Path:** `src/lib/ai/adapters/DigitalHumanProviderAdapter.ts` (To be implemented).
- **Environment Requirements:** 
  - `DIGITAL_HUMAN_API_URL`
  - `DIGITAL_HUMAN_API_KEY` (MUST NEVER be exposed to client).
- **Current Status:** `MISSING_CREDENTIALS`.
- **Classification:** NEW.
- **Production Readiness:** `NOT_READY` (Blocked pending provider selection and credentials).

## FALLBACK CAPABILITY
- **Text-Only Fallback:** Proven by architecture lock. If media session fails or is denied, the Shared AI Core continues responding via text using the identical `AiConversation` and `AiServiceSession`.

## CONTRACT LOCK
1. All provider-specific WebRTC/streaming APIs are isolated within `DigitalHumanProviderAdapter`.
2. The client receives short-lived, scoped tokens generated by the `AvatarSessionBroker`.
3. Permanent credentials reside strictly server-side.
4. If media fails, the frontend disables avatar components and renders text-only responses.
