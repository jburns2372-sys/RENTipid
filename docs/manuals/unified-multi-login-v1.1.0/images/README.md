# Supporting Images

Only privacy-safe images may be stored here. Images must not contain email addresses, phone numbers, account identifiers, OAuth artifacts, session data, credentials or other personal information.

The public sign-in screenshot used by this manual suite is captured without authenticating and is named `unified-login-page.png`. Account Security screenshots are intentionally omitted because obtaining them would require a live user session and could expose account data.
